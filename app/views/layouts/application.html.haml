%html
  %head
    %title Activid
    = stylesheet_link_tag    "application"#, media: "all", "data-turbolinks-track" => true
    = javascript_include_tag "application"#, "data-turbolinks-track" => true
    %script(type='text/javascript' src='https://js.stripe.com/v2/')
    = csrf_meta_tags
  %body
    .container
      %header
        %h1
          Activid
          %span.page-title
            =# Only one of these will have a value at any given time.
            = yield(:page_title)
            = @cms_page.try(:label)
      = yield

      %footer
        %nav
          - cms_info_pages.each do |page|
            = link_to page.label, page.full_path
      .ajax-spinner(style='display: none;')
        AJAX IS HAPPENING...

    :javascript
      Stripe.setPublishableKey(#{STRIPE_PUBLISHABLE_KEY.to_json})